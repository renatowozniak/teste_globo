# Como executar o projeto

**docker-compose up --build -d**

# Descrição

O projeto está com **2 tipos de cache** com o intuito de apenas mostrar as possibilidades que podem ser utilizadas em aplicações.
